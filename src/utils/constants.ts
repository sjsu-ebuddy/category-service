export const config = Object.freeze({
    PORT: 8084,
    HOST: "0.0.0.0",
    SESSION_SECRET: "$NO SE CUAL SECRET$",
});

export const env = Object.freeze({
    PROD: "production",
    DEV: "development",
    QA: "test"
});